<%=@book.title%>
<br>
<%= image_tag @book.book_cover.url(:medium) %>
<br>
<%= link_to "Home Page", root_path %>
<%= button_to "Delete Book", { action: :destroy, id: @book.id },
                                method: :destroy, data: { confirm: "Are you sure?" } %>